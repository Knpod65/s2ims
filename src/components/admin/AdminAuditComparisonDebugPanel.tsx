'use client'

// AP-9G Stage 1 hidden component skeleton.
// Returns null for all inputs — no DOM output, no Admin UI integration,
// no prototype read switch, no PII. Stage 2 rendering requires explicit approval.

type AdminAuditComparisonDebugPanelProps = {
  enabled?: boolean
  isAdmin?: boolean
  variant?: 'hidden'
}

export function AdminAuditComparisonDebugPanel(
  _props: AdminAuditComparisonDebugPanelProps,
) {
  return null
}

export default AdminAuditComparisonDebugPanel
